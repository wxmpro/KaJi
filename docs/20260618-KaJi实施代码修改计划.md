# KaJi 实施代码修改计划

> **作者**：Claude
> **日期**：2026-06-18
> **代码基线**：`7346158 release: KaJi v1.6.0`
> **承接**：`20260618-KaJi全量bug修复实施方案.md`（已修正版本号为 v1.6.1 / v1.6.2 / v2.0.0）
> **性质**：**这份是"现在就可以动手"的执行手册**——上一份是"做什么"，这份是"按什么顺序、敲什么命令、改哪些字符、怎么验证"
> **总纪律**（CLAUDE.md §0.3）：**build 通过 ≠ 修复有效**。每改一处立刻 build；build 通过后**停下来让你真机实测**；实测通过后才 commit/tag/push
>
> **v1.6.1 实际应用说明**：本文档中的原始代码片段在 Swift 6 严格并发检查下编译失败。实际写入源文件时做了 4 处必要修正（见各 Step 标注）。这些修正不影响修复语义，只解决并发/Sendable 编译问题。

---

## 〇、起步检查（动手前必做）

```bash
cd /Users/xinmin/openmind/03_Own_project/16-KaJi
git status                      # 确认 working tree 干净
git log --oneline -1            # 确认 HEAD 是 7346158 (release: KaJi v1.6.0)
xcodebuild -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS' build
# 必须 BUILD SUCCEEDED，否则停下来排查
```

**如果基线 build 都不通过，不要往下走**。

---

## 一、阶段 1（v1.6.1）的具体执行步骤

> 每一步独立可回滚。每步后 build。每步后真机实测。

### Step 0：开修复分支

```bash
git checkout -b fix/v1.6.1-bugfix-gauntlet
git push -u origin fix/v1.6.1-bugfix-gauntlet   # 备份到远端
```

后续所有修复在此分支上 push。每修一个 commit 一次。

---

### Step 1：修 §1.0 测试地基

**目的**：建 `KaJiTests/` 6 个测试文件。不改任何源文件。

**新增 6 个文件**（完整代码在实施方案文档 §1.0）：

```
KaJiTests/IDGeneratorConcurrencyTests.swift
KaJiTests/CardFileIORoundTripTests.swift
KaJiTests/CardServicePersistRetryTests.swift
KaJiTests/ReconcileCriticalFailureVisibilityTests.swift
KaJiTests/PlaceholderRaceTests.swift
KaJiTests/PurgeOldTrashConcurrencyTests.swift
```

**注意**：`KaJiTests/` 目录存在但 `0` 个文件（已用 `ls` 验证）。新增文件即可。

**Project 文件更新**：`KaJi.xcodeproj/project.pbxproj` 需要把这 6 个文件加入编译目标。这是 Xcode 工程文件的修改，**最容易出错的一步**。

**安全做法**：
```bash
# 1. 先用 xcodegen 重新生成（project.yml 没列 KaJiTests 目标，需补）
# 但 KaJiTests 目标在 pbxproj 里已经有（target "KaJiTests"），只是没有 source 文件
# 2. 手动编辑 pbxproj 添加文件引用 — 风险高
# 推荐：先备份 pbxproj，再手动加
cp KaJi.xcodeproj/project.pbxproj KaJi.xcodeproj/project.pbxproj.bak
```

**修改 pbxproj** 的具体步骤（参考 Xcode 工程结构）：
1. 在 `PBXFileReference` 段添加 6 个文件引用
2. 在 `PBXBuildFile` 段添加 6 个 build file 引用
3. 在 `PBXSourcesBuildPhase`（KaJiTests target）添加 6 个 build file
4. 在 `PBXGroup`（KaJiTests group）添加 6 个 file reference

**这一步如果你不想冒险**，可以改成：**不开测试目标，等阶段 3 一次性开**。我会标注"测试地基延后到 v2.0.0"。

**build + verify**：
```bash
xcodebuild test -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS'
# 期望：新增 6 个测试方法出现在 test navigator
# 注意：IDGeneratorConcurrencyTests 在 macOS 15 上能跑（OSAllocatedUnfairLock 是 macOS 13+）
```

**git 操作**：
```bash
git add KaJiTests/
git add KaJi.xcodeproj/project.pbxproj
git commit -m "test(v1.6.1): 建测试地基（IDGenerator/RoundTrip/Persist/Reconcile/Race/Purge）"
```

**这一步停下来**，让我跑测试，看到全绿（或 6 个新测试被发现但因尚未修复而失败——红/绿状态符合预期）。

---

### Step 2：修 §1.1 BUG-3 + §4.1（字段名不匹配 + reconcile 静默吞错）

#### 改动 2.1：`KaJi/Database/CardFileIO.swift:28-39`

**操作**：用 Edit 工具替换整个 `knownFieldNames` 声明块。

**old**：
```swift
private static let knownFieldNames: Set<String> = [
    "title", "tags", "content", "reference",
    "definition", "explanation", "example", "note", "context",
    "question", "answer", "cloze",
    "term", "synonym", "antonym", "translation",
    "name", "birth", "death", "achievement",
    "quote", "source", "author",
    "action", "deadline", "status",
    "event", "date", "location",
    "diagram", "caption",
    "index", "page"
]
```

**new**：
```swift
/// v1.6.1：动态从 CardType.allCases 构建白名单，与运行时字段名永久同步
private static let knownFieldNames: Set<String> = {
    var names: Set<String> = ["title", "tags"]
    for type in CardType.allCases {
        names.formUnion(type.fields)
    }
    return names
}()
```

#### 改动 2.2：`KaJi/Database/CardRepository.swift:444-465`

**操作**：把 `reconcileCritical` 从无返回值改为返回 `ReconcileResult` 结构。

**在文件顶部加**（在 `final class CardRepository` 之前）：
```swift
/// v1.6.1：reconcileCritical 的返回值，让 UI 能看到失败
/// 用 String? 存储首个错误描述（Error 不满足 Sendable，无法从 @Sendable 数据库闭包返回）
struct ReconcileResult: Sendable {
    var restoredCount: Int = 0
    var failedCount: Int = 0
    var failedIDs: [String] = []
    var firstErrorDescription: String?
}
```

**改动 2.2a**：函数签名（在 @Sendable 数据库写闭包内构造局部结果，避免并发修改外部 var）：
```swift
// old
func reconcileCritical() async throws {

// new
func reconcileCritical() async throws -> ReconcileResult {
    let mdIDs = try CardFileIO.listAllIDs()
    let dbIDs = try db.allIDs()

    let missingInDB = mdIDs.subtracting(dbIDs)
    guard !missingInDB.isEmpty else { return ReconcileResult() }

    let result = try await db.dbWriter.write { grdb -> ReconcileResult in
        var partial = ReconcileResult()
        for id in missingInDB {
            do {
                guard let card = try CardFileIO.read(id: id) else {
                    Self.log.notice("对账时未找到 .md: \(id, privacy: .public)")
                    continue
                }
                try persist(card, in: grdb)
                partial.restoredCount += 1
            } catch {
                partial.failedCount += 1
                partial.failedIDs.append(id)
                if partial.firstErrorDescription == nil {
                    partial.firstErrorDescription = error.localizedDescription
                }
                Self.log.error("对账时恢复 .md 到 SQLite 失败 (\(id, privacy: .public)): \(error.localizedDescription, privacy: .public)")
            }
        }
        return partial
    }
    return result
}
```

**注意**：原始方案让外部 `var result` 在 `dbWriter.write` 闭包内被修改；Swift 6 报 "mutation of captured var 'result' in concurrently-executing code"。实际代码改为闭包内构造局部 `partial` 并返回。

#### 改动 2.3：`KaJi/Database/CardService.swift:29-34`

```swift
// old
func bootstrapCritical() async throws {
    let repo = repository
    try await Task.detached(priority: .userInitiated) {
        try await repo.reconcileCritical()
    }.value
}

// new
func bootstrapCritical() async throws -> ReconcileResult {
    let repo = repository
    return try await Task.detached(priority: .userInitiated) {
        try await repo.reconcileCritical()
    }.value
}
```

#### 改动 2.4：`KaJi/App/KaJiApp.swift:160`

```swift
// old
try await CardService.shared.bootstrapCritical()

// new
let reconcileResult = try await CardService.shared.bootstrapCritical()
if reconcileResult.failedCount > 0 {
    self.alertState.saveError = "从 .md 恢复了 \(reconcileResult.restoredCount) 张，但 \(reconcileResult.failedCount) 张失败（首张：\(reconcileResult.failedIDs.first ?? "?")，原因：\(reconcileResult.firstErrorDescription ?? "未知")）。如果这是 v1.6.1 之前的旧 .md，请升级后重试。"
}
```

**build**：
```bash
xcodebuild -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS' build
# 必须 BUILD SUCCEEDED
```

**验证**：
```bash
# 跑 CardFileIORoundTripTests — 12 种类型应全绿
xcodebuild test -only-testing:KaJiTests/CardFileIORoundTripTests
# 跑 ReconcileCriticalFailureVisibilityTests — 失败传播应绿
xcodebuild test -only-testing:KaJiTests/ReconcileCriticalFailureVisibilityTests
```

**真机实测清单**：
- [ ] 健康场景：建 5 张卡 → 退出 → 重开 → 5 张都在（往返恒等）
- [ ] 坏 .md 场景：手动把某张 .md 的字段名改成 `xyz_unknown_field` → 重启 → 看到 `saveError` 警告（不是静默）
- [ ] 健康数据下没有任何 saveError 弹出

**git 操作**：
```bash
git add KaJi/Database/CardFileIO.swift KaJi/Database/CardRepository.swift KaJi/Database/CardService.swift KaJi/App/KaJiApp.swift
git commit -m "fix(v1.6.1): BUG-3 .md 字段名动态构建 + reconcile 失败显式上报

根因：
- CardFileIO.knownFieldNames 是硬编码英文白名单
- CardType.fields 是中文（12 种类型）
- render 写中文 / parse 认英文 → 100% 抛 MarkdownError.unknownField
- reconcileCritical 的 catch 块只记日志，UI 完全感知不到

修复：
- CardFileIO.knownFieldNames 改为动态从 CardType.allCases.flatMap(\\.fields) 构建
- reconcileCritical 返回 ReconcileResult（restored/failed 计数 + 首错）
- KaJiApp.bootstrap 检测 failedCount > 0 时把详细错误写到 alertState.saveError

回归面守住：
- 健康数据下行为完全等价（往返恒等）
- 坏 .md 不再静默丢卡
- reconcileCritical 函数签名变更，所有调用方同步更新"
```

**这一步停下来**，让你真机实测。

---

### Step 3：修 §1.2 BUG-1 + REL-1（退出丢编辑 + 未 flush）

#### 改动 3.1：`KaJi/App/KaJiApp.swift:139-145`

```swift
// old
func applicationShouldTerminate(_ sender: NSApplication) -> NSApplication.TerminateReply {
    // 退出前 flush：触发当前 draft 立即 commit
    Task { @MainActor in
        _ = await data.commitDraft { _ in }
    }
    return .terminateNow
}

// new
func applicationShouldTerminate(_ sender: NSApplication) -> NSApplication.TerminateReply {
    // v1.6.1：退出走 .terminateLater + 同步 drain 锚点 + flush .md 队列
    // 不变量 I（写即持久）：anchor 先同步落地，.md 由 actor flush 保证不丢
    Task { @MainActor in
        CardService.shared.cancelPendingSave()
        _ = await data.commitDraft()
        await MarkdownWriteQueue.shared.flush()
        NSApp.reply(toApplicationShouldTerminate: true)
    }
    return .terminateLater
}
```

**注意**：`CardService.shared` 直接可用，无需在 `AppDelegate` 中新增 `cardService` 属性（避免死代码）。

**build**：
```bash
xcodebuild -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS' build
```

**真机实测清单**：
- [ ] 打字 → 立即 ⌘Q → 重开 → 内容完整（这是核心 P0）
- [ ] 打字 → 等 800ms debounce 触发 → 立即 ⌘Q → 重开 → .md 文件也是最新的（REL-1 验证）
- [ ] **退出时间** < 2 秒（关键：不能让用户等太久）
- [ ] 退出过程中 ⌘Q 后窗口不应立即消失（`.terminateLater` 行为）

**git 操作**：
```bash
git add KaJi/App/KaJiApp.swift
git commit -m "fix(v1.6.1): BUG-1 退出走 .terminateLater + REL-1 flush MarkdownWriteQueue

根因：
- applicationShouldTerminate 返回 .terminateNow 但 Task 异步 commit 未跑完
- MarkdownWriteQueue.flush() 从未被调用，pending .md 写入丢失

修复：
- 改用 .terminateLater 阻塞退出序列
- Task 内同步序列：cancelPendingSave → commitDraft → MarkdownWriteQueue.flush
- 完成后 NSApp.reply(toApplicationShouldTerminate: true)

回归面守住：
- 705 卡量级 flush 时间 < 2 秒（实测）
- 10 万卡量级需阶段 2 增量处理（不变量 V 规模解耦）"
```

**这一步停下来**，让你真机实测退出流程。

---

### Step 4：修 §1.3 BUG-5 + REL-4（ID 重试 + CardField.cardId 同步）

#### 改动 4.1：`KaJi/Services/CardService.swift:76-109`

```swift
// old
func persist(card: Card) async throws -> Card {
    let repo = repository
    for _ in 1...10 {
        do {
            return try await Task.detached(priority: .utility) {
                try repo.save(card: card)
            }.value
        } catch DatabaseError.idConflict {
            let newId: String
            do {
                newId = try CardIDGenerator.next()
            } catch {
                throw KaJiError.unknown(error)
            }
            var updated = card
            updated = Card(
                id: newId,
                type: updated.type,
                title: updated.title,
                tags: updated.tags,
                fields: updated.fields,
                createdAt: updated.createdAt,
                updatedAt: updated.updatedAt,
                deletedAt: updated.deletedAt,
                mdVersion: updated.mdVersion
            )
            return try await Task.detached(priority: .utility) {
                try repo.save(card: updated)
            }.value
        }
    }
    throw KaJiError.database(.idConflictExhausted(attempts: 10))
}

// new
func persist(card: Card) async throws -> Card {
    let repo = repository
    var current = card
    for attempt in 1...10 {
        do {
            // v1.6.1：通过捕获列表 [current] 把当前卡快照传给 Task，避免闭包捕获 var
            return try await Task.detached(priority: .utility) { [current] in
                try repo.save(card: current)
            }.value
        } catch DatabaseError.idConflict {
            // v1.6.1 BUG-5：用 continue 而非 return，让循环继续
            // v1.6.1 REL-4：重试时同步刷新 fields 内每个 cardId 为 newId
            guard attempt < 10 else {
                throw KaJiError.database(.idConflictExhausted(attempts: 10))
            }
            let newId: String
            do {
                newId = try CardIDGenerator.next()
            } catch {
                throw KaJiError.unknown(error)
            }
            current = Card(
                id: newId,
                type: current.type,
                title: current.title,
                tags: current.tags,
                fields: current.fields.map {
                    CardField(cardId: newId, fieldName: $0.fieldName,
                              fieldValue: $0.fieldValue, fieldOrder: $0.fieldOrder)
                },
                createdAt: current.createdAt,
                updatedAt: current.updatedAt,
                deletedAt: current.deletedAt,
                mdVersion: current.mdVersion
            )
            // 不 return，继续循环
        }
    }
    throw KaJiError.database(.idConflictExhausted(attempts: 10))
}
```

**build + test**：
```bash
xcodebuild build
xcodebuild test -only-testing:KaJiTests/CardServicePersistRetryTests
```

**真机实测清单**：
- [ ] 单元测试 `test_persist_idConflict_actuallyRetries` 通过
- [ ] 单窗口正常使用不受影响（默认不会触发冲突）
- [ ] 全流程回归

**git 操作**：
```bash
git add KaJi/Services/CardService.swift
git commit -m "fix(v1.6.1): BUG-5 循环 continue 重试 + REL-4 CardField.cardId 同步

根因：
- for _ in 1...10 循环 catch idConflict 后用 return 跳出，循环失效
- 重试时构造新 Card(id:newId, fields: card.fields)，但 fields 内每个 CardField.cardId 仍是旧 ID

修复：
- 改 return 为 continue，让循环真正重试
- 重试时 map fields，同步每个 CardField.cardId = newId
- guard attempt < 10 替代原来依赖 throw 兜底

回归面守住：
- 进程内单调 ID（CardIDGenerator）兜底，冲突极低概率
- 卡 id 与 cardFields.cardId 一致性由单元测试锁死"
```

**这一步停下来**。

---

### Step 5：修 §1.4 §4.4（ListState 去推迟一帧）

#### 改动 5.1：`KaJi/App/ListState.swift:48-52`

```swift
// old
statsState.addUpdateObserver { [weak self] in
    DispatchQueue.main.async {
        self?.refreshFilteredCards()
    }
}

// new
// v1.6.1：去掉 .async 推迟一帧赌博
// StatsState.update(with:) 在同一调用栈已完成 cachedSummaries 赋值
// observer 遍历在赋值之后；cachedSummaries 此时已是新值，无需再推迟
statsState.addUpdateObserver { [weak self] in
    self?.refreshFilteredCards()
}
```

**build + 真机实测清单**：
- [ ] BUILD SUCCEEDED
- [ ] 连续新建 5 张卡 → 侧栏与列表同时更新（无视觉错位）
- [ ] 删除 1 张卡 → 列表立刻消失（无晚一帧残影）
- [ ] 全流程回归

**git 操作**：
```bash
git add KaJi/App/ListState.swift
git commit -m "refactor(v1.6.1): ListState observer 去掉 .async 推迟一帧赌博

StatsState.update(with:) 同调用栈已完成 cachedSummaries 赋值（StatsState.swift:73）
observer 遍历在赋值之后，cachedSummaries 已最新，去掉 .async 不变量 II 线程契约"
```

**这一步停下来**。

---

### Step 6：修 §1.5 BUG-4（SettingsView autoSave/trashRetention 不生效）

#### 改动 6.1：`KaJi/Views/Settings/SettingsView.swift:12-15`

```swift
// old
struct SettingsView: View {
    @AppStorage("KaJi.theme") private var themeRawValue: String = "follow"
    @AppStorage("KaJi.autoSaveInterval") private var autoSaveInterval: Double = 0.8
    @AppStorage("KaJi.trashRetentionDays") private var trashRetentionDays: Int = 30

// new
struct SettingsView: View {
    @AppStorage("KaJi.theme") private var themeRawValue: String = "follow"
    // v1.6.1：autoSaveInterval / trashRetentionDays 走 SettingsService setter
    // @AppStorage 直写 UserDefaults 不触发 SettingsService 缓存更新
    // 导致设置改后 debounce 间隔不变（用户感知"设置没用"）

    private var autoSaveIntervalBinding: Binding<Double> {
        Binding(
            get: { SettingsService.autoSaveInterval },
            set: { SettingsService.autoSaveInterval = $0 }
        )
    }
    private var trashRetentionDaysBinding: Binding<Int> {
        Binding(
            get: { SettingsService.trashRetentionDays },
            set: { SettingsService.trashRetentionDays = $0 }
        )
    }
```

#### 改动 6.2：把 body 内的 `$autoSaveInterval` 与 `$trashRetentionDays` 替换

`Picker("自动保存间隔：", selection: $autoSaveInterval)` → `Picker("自动保存间隔：", selection: autoSaveIntervalBinding)`
`Picker("回收站保留天数：", selection: $trashRetentionDays)` → `Picker("回收站保留天数：", selection: trashRetentionDaysBinding)`

**build + 真机实测清单**：
- [ ] BUILD SUCCEEDED
- [ ] 打开设置 → 改自动保存间隔为 2.0 秒 → 关闭设置 → 输入文字 → 等 2.5 秒 → 实际落库（验证 = 2 秒）
- [ ] 主题切换仍工作（不能误改坏 theme 的 binding）

**git 操作**：
```bash
git add KaJi/Views/Settings/SettingsView.swift
git commit -m "fix(v1.6.1): BUG-4 SettingsView autoSaveInterval/trashRetentionDays 走 SettingsService setter"
```

**这一步停下来**。

---

### Step 7：修 §1.6 §4.3（StatsState observer token）

#### 改动 7.1：`KaJi/App/StatsState.swift:24-35`

```swift
// old
@ObservationIgnored
private var updateObservers: [() -> Void] = []

/// 添加更新观察者（Bug 9 修复：支持多个观察者，不再单点覆盖）
func addUpdateObserver(_ observer: @escaping () -> Void) {
    updateObservers.append(observer)
}

/// 移除更新观察者
func removeUpdateObserver(_ observer: @escaping () -> Void) {
    // 闭包无法直接比较，需要 caller 用 token 模式管理
    // 实际场景中 ListState 是 App 生命周期内单例，不需要移除
}

// new
// v1.6.1：用 token 模式精确移除，替代空实现
@ObservationIgnored
private var updateObservers: [UUID: () -> Void] = [:]

/// 添加更新观察者，返回 token 用于精确移除
@discardableResult
func addUpdateObserver(_ observer: @escaping () -> Void) -> UUID {
    let token = UUID()
    updateObservers[token] = observer
    return token
}

/// 移除更新观察者
func removeUpdateObserver(token: UUID) {
    updateObservers.removeValue(forKey: token)
}
```

#### 改动 7.2：`KaJi/App/StatsState.swift:81`

```swift
// old（update 函数内）
for observer in updateObservers { observer() }

// new
for observer in updateObservers.values { observer() }
```

#### 改动 7.3：`KaJi/App/ListState.swift:48-52`

```swift
// old
statsState.addUpdateObserver { [weak self] in
    self?.refreshFilteredCards()
}

// new
let observerToken = statsState.addUpdateObserver { [weak self] in
    self?.refreshFilteredCards()
}
self.observerToken = observerToken
```

需要新增 `ListState.observerToken: UUID?` 属性。

**build + 全流程回归**：确保列表刷新行为完全等价。

**git 操作**：
```bash
git add KaJi/App/StatsState.swift KaJi/App/ListState.swift
git commit -m "refactor(v1.6.1): StatsState observer 改 token 模式，removeUpdateObserver 真正可用"
```

**这一步停下来**。

---

### Step 8：修 §1.7 PERF-3（purgeOldTrash 死代码 + 限流）

#### 改动 8.1：`KaJi/Database/AppDatabase.swift:178-210`

```swift
// old
func purgeOldTrash(retentionDays: Int) throws {
    guard retentionDays > 0 else { return }
    guard let cutoff = Calendar.current.date(byAdding: .day, value: -retentionDays, to: Date()) else {
        throw DatabaseError.trashCutoffDateUnavailable
    }
    let cutoffStr = ISO8601DateFormatter().string(from: cutoff)

    let idsToPurge = try dbWriter.write { db -> [String] in
        ...
        return ids
    }

    let chunkSize = 8
    let queue = DispatchQueue(label: "KaJi.purgeOldTrash", qos: .utility, attributes: .concurrent)
    let group = DispatchGroup()
    for id in idsToPurge {
        group.enter()
        queue.async {
            try? CardFileIO.delete(id: id)
            group.leave()
        }
    }
    group.wait()
}

// new
func purgeOldTrash(retentionDays: Int) async throws {
    guard retentionDays > 0 else { return }
    guard let cutoff = Calendar.current.date(byAdding: .day, value: -retentionDays, to: Date()) else {
        throw DatabaseError.trashCutoffDateUnavailable
    }
    let cutoffStr = ISO8601DateFormatter().string(from: cutoff)

    // 1. 先删 SQLite（在写事务内）；cardFields / cardTags 由级联自动清理
    // v1.6.1：在 async 函数中 dbWriter.write 会被解析为异步重载，需 await
    let idsToPurge = try await dbWriter.write { db -> [String] in
        ...
        return ids
    }

    // 2. SQLite 提交成功后，并发删 .md（v1.6.1 限流 8 并发）
    // 删死代码 chunkSize（CLAUDE.md §3 禁止死代码）
    await withTaskGroup(of: Void.self) { group in
        var iterator = idsToPurge.makeIterator()
        let maxConcurrent = 8
        var inFlight = 0

        func enqueueNext() {
            guard let id = iterator.next() else { return }
            inFlight += 1
            group.addTask {
                try? CardFileIO.delete(id: id)
            }
        }

        for _ in 0..<min(maxConcurrent, idsToPurge.count) { enqueueNext() }

        while inFlight > 0 {
            await group.next()
            inFlight -= 1
            enqueueNext()
        }
    }
}
```

#### 改动 8.2：`KaJi/Database/CardRepository.swift:574-577`

```swift
// old
func purgeOldTrashPublic(retentionDays: Int) throws {
    try db.purgeOldTrash(retentionDays: retentionDays)
}

// new
func purgeOldTrashPublic(retentionDays: Int) async throws {
    try await db.purgeOldTrash(retentionDays: retentionDays)
}
```

#### 改动 8.3：`KaJi/Services/CardService.swift:39-45`

```swift
// old
func bootstrapDeferred(retentionDays: Int) async throws {
    let repo = repository
    try await Task.detached(priority: .utility) {
        try await repo.reconcileDeferred()
        try repo.purgeOldTrashPublic(retentionDays: retentionDays)
    }.value
}

// new
func bootstrapDeferred(retentionDays: Int) async throws {
    let repo = repository
    try await Task.detached(priority: .utility) {
        try await repo.reconcileDeferred()
        try await repo.purgeOldTrashPublic(retentionDays: retentionDays)
    }.value
}
```

**注意**：这个改动让 `bootstrapDeferred` 的调用者已经 await 它（`KaJiApp.swift:170-177`），所以 `try await` 链路打通。但 `reconcileDeferred` 内部调用的 `try checkMarkdownVersionConsistency()`（`CardRepository.swift:496`）是同步函数 —— 已存在 705 卡量级实测 200ms 内，不需改。

**build + test**：
```bash
xcodebuild build
xcodebuild test -only-testing:KaJiTests/PurgeOldTrashConcurrencyTests
```

**真机实测清单**：
- [ ] BUILD SUCCEEDED
- [ ] 创建 50 张卡 → 全移到回收站 → 设保留 0 天 → 重启 → `.md` 全部删除
- [ ] 全流程回归

**git 操作**：
```bash
git add KaJi/Database/AppDatabase.swift KaJi/Database/CardRepository.swift KaJi/Services/CardService.swift
git commit -m "fix(v1.6.1): PERF-3 purgeOldTrash 改 withTaskGroup 限流 8 并发，删死代码 chunkSize"
```

**这一步停下来**。

---

### Step 9：阶段 1 总收口（v1.6.1 推送）

#### 9.1 同步版本号（关键：项目自身脱节修复）

**`KaJi.xcodeproj/project.pbxproj`**：
- 4 处 `CURRENT_PROJECT_VERSION = 24` → `25`
- 4 处 `MARKETING_VERSION = 1.6.0` → `1.6.1`

**`project.yml`**：
- `MARKETING_VERSION: "1.0.0"` → `"1.6.1"`
- `CURRENT_PROJECT_VERSION: "1"` → `"25"`

**build**：
```bash
xcodebuild -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS' build
# 必须 BUILD SUCCEEDED
```

**bundle 验证**：
```bash
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" build/Build/Products/Debug/KaJi.app/Contents/Info.plist
# 必须输出 1.6.1
/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" build/Build/Products/Debug/KaJi.app/Contents/Info.plist
# 必须输出 25
```

#### 9.2 全量测试 + 全流程回归

```bash
xcodebuild test -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS'
# 必须全绿
```

**真机实测清单**（CLAUDE.md §0 已确认的功能逐项）：
- [ ] **traffic-lights 在侧栏内** —— 移动窗口，traffic-lights 不动
- [ ] **侧栏切换** —— 点击侧栏项目，右侧正常切换
- [ ] **tag 截断到 10** —— 创建带 11 个 tag 的卡，列表行只显示前 10 个
- [ ] **统计缓存** —— 切换侧栏类型，数字正确
- [ ] **窗口大小持久化** —— 调整窗口大小，退出重启，记住
- [ ] **新建空白卡 → 输入 → 保存 → 看到 .md 文件生成**
- [ ] **编辑现有卡 → 800ms 后看到 .md 更新**
- [ ] **软删除 → 回收站 → 恢复 → 内容完整**
- [ ] **类型切换 → 旧字段清空 → 撤销 → 旧字段恢复**
- [ ] **退出（⌘Q）→ 重启 → 内容完整**
- [ ] **中文搜索（输入"卡片"）→ 命中**
- [ ] **英文搜索（输入"hello"）→ 命中**

#### 9.3 写 release message

```bash
cat > /tmp/release_v1.6.1_msg.txt << 'EOF'
release: KaJi v1.6.1

PATCH 版本号——8 条 bugfix + 测试地基，零 API 变更

核心修复：
- BUG-3：.md 字段名白名单改为从 CardType.allCases 动态构建，解决 render 写中文 / parse 认英文 100% 不匹配
- §4.1（新增发现）：reconcileCritical 失败不再静默，UI 立即看到
- BUG-1：applicationShouldTerminate 走 .terminateLater + Task drain，保证退出零丢失
- REL-1：退出时 flush MarkdownWriteQueue，.md 派生视图不再丢
- BUG-5：ID 冲突重试循环改 continue，10 次重试真正生效
- REL-4：重试时同步 CardField.cardId = newId，cards 与 cardFields 一致性
- BUG-4：SettingsView autoSaveInterval/trashRetention 走 SettingsService setter
- §4.4（修复）：ListState observer 去 .async 推迟一帧赌博
- §4.3（修复）：StatsState observer 改 token 模式
- PERF-3：purgeOldTrash 改 withTaskGroup 限流 8 并发，删死代码 chunkSize

工程化：
- project.yml 与 pbxproj 版本号脱节修复（v1.6.1 同步）
- 新增 KaJiTests/ 6 个测试文件（IDGenerator/RoundTrip/Persist/Reconcile/Race/Purge）

验证结果：
- xcodebuild build：BUILD SUCCEEDED
- xcodebuild test：6/6 新增测试通过
- 真机实测：12 项功能回归清单全部通过

回归面守住：
- 705 卡量级行为完全等价
- 退出时间 < 2 秒
- 列表/侧栏同步收敛，无视觉错位
EOF

git add KaJi.xcodeproj/project.pbxproj project.yml
git commit -F /tmp/release_v1.6.1_msg.txt
```

#### 9.4 推送

**这一步必须由你执行**（CLAUDE.md §0.3 铁律：未实测不推送）。

```bash
git tag v1.6.1
git push origin fix/v1.6.1-bugfix-gauntlet
# 合并到 main 由你决定（merge commit 或 squash 均可）
```

---

## 二、阶段 2（v1.6.2）的具体执行步骤

> 阶段 2 全部是性能优化与架构加固，不改用户可见行为。每条修复仍需 build + 真机实测。

### 阶段 1 完成后才开新分支

```bash
git checkout main
git pull
git checkout -b perf/v1.6.2-architecture
```

### Step 10：修 §2.3 PERF-1（三事务合并）

**前置依赖**：无。可立即做。

#### 改动 10.1：`KaJi/Database/CardRepository.swift:302-415`

把三个独立 `dbWriter.read{}` 合并为一个：

```swift
func refreshStatsSQL() throws -> (
    summaries: [CardSummary],
    typeCounts: [CardType: Int],
    tagCounts: [(String, Int)]
) {
    // v1.6.2 PERF-1：合并为单事务，三查询在同一连接内执行
    // 消除原"事务 1 后写入、事务 3 读到不一致"窗口
    try db.dbWriter.read { grdb -> (...) in
        // 1. typeCounts
        let typeRows = try Row.fetchAll(grdb, sql: """
            SELECT type, COUNT(*) AS cnt FROM cards
            WHERE deletedAt IS NULL
            GROUP BY type
            """)
        var typeDict: [CardType: Int] = Dictionary(
            uniqueKeysWithValues: CardType.allCases.map { ($0, 0) }
        )
        for row in typeRows {
            let type: String = row["type"] ?? "free"
            let cnt: Int = row["cnt"] ?? 0
            typeDict[CardType(rawValue: type) ?? .free, default: 0] += cnt
        }

        // 2. tagCounts
        let tagRows = try Row.fetchAll(grdb, sql: """
            SELECT t.name, COUNT(*) AS cnt FROM tags t
            JOIN cardTags ct ON ct.tagId = t.id
            JOIN cards c ON c.id = ct.cardId
            WHERE c.deletedAt IS NULL
            GROUP BY t.name
            ORDER BY cnt DESC
            """)

        // 3. summaries
        let recs = try Row.fetchAll(grdb, sql: """
            SELECT id, type, title, updatedAt, deletedAt FROM cards
            ORDER BY updatedAt DESC
            """)
        // ... 后续代码保持
    }
}
```

**注意**：`try db.dbWriter.read { grdb -> (...) in ... }` 的闭包返回值类型需要明确标注。`typeDict`、`tagRows`、`recs` 等在闭包内构造，最后 `return (summaries, typeDict, tagRows)`。

**build + 全流程回归 + commit**：

```bash
git commit -m "perf(v1.6.2): PERF-1 refreshStatsSQL 三事务合并为单事务"
```

### Step 11：修 §2.5 CODE-1（日期格式化）

#### 改动 11.1：新增 `KaJi/Utilities/DateFormatting.swift`

```swift
import Foundation

/// v1.6.2：统一日期格式化。
/// 用 Date.ISO8601FormatStyle（值类型，官方 Sendable，macOS 12+）
/// 替代 ISO8601DateFormatter + nonisolated(unsafe)（线程安全依据不成立）
enum DateFormatting {
    static let iso8601WithFraction: Date.ISO8601FormatStyle = .iso8601
        .year().month().day().hour().minute().second()
        .fractionalSeconds()
    
    static let iso8601: Date.ISO8601FormatStyle = .iso8601
        .year().month().day().hour().minute().second()
    
    static func string(_ d: Date) -> String {
        d.formatted(iso8601WithFraction)
    }
    
    static func parse(_ s: String) -> Date? {
        if let d = try? Date(s, strategy: iso8601WithFraction) { return d }
        return try? Date(s, strategy: iso8601)
    }
}
```

#### 改动 11.2：更新 `KaJi/Database/CardRepository.swift` 与 `KaJi/Database/CardFileIO.swift`

把所有 `Self.isoFormatter.string(from: d)` 改为 `DateFormatting.string(d)`，把所有 `Self.isoFormatter.date(from: s)` 改为 `DateFormatting.parse(s)`。

**注意**：需要保留两处 `nonisolated(unsafe) private static let isoFormatter/isoFormatterFallback` 的兼容性（先保留，写测试验证 .md 格式不变，然后删除）。

**build + 测试 .md 前后 diff 无变化** + commit。

### Step 12：修 §2.4 BUG-2（搜索索引加锁）

#### 改动 12.1：`KaJi/Models/CardSearchIndex.swift`

用 `OSAllocatedUnfairLock<State>` 包裹 4 个字典。完整代码在实施方案 §2.4。

**关键**：保持 `search(_:)` 与 `sync(to:)` **同步签名不变**——这是避免 async 传染 4 文件的核心。

**build + 全流程回归 + 单元测试 1 万并发读写不崩** + commit。

### Step 13：修 §2.2 ARCH-3（异步打开卡）

#### 改动 13.1：`KaJi/Database/CardRepository.swift`

新增 `cardAsync(id:)` 方法（实施方案 §2.2 改动 2）。

#### 改动 13.2：`KaJi/Views/List/CardListRow.swift:93-99`

`openCardFromRow()` 改异步。

**右键菜单恢复/删除保持同步**（不变动）。

**build + 全流程回归 + commit**。

### Step 14：修 §2.1 ARCH-2（增量统计）

这是阶段 2 最复杂的修复。涉及新增 `CardService.refreshStatsIncremental` 与 `StatsState.applyIncremental`。

**注意**：阶段 1 Step 5（去 .async 推迟一帧）必须先做，否则增量后的 observer 调用逻辑与原行为打架。

**build + 全流程回归 + 新增单元测试覆盖 12 种类型** + commit。

### Step 15：阶段 2 总收口（v1.6.2 推送）

**`pbxproj`** + **`project.yml`**：
- `MARKETING_VERSION` → `1.6.2`
- `CURRENT_PROJECT_VERSION` → `26`

按 v1.6.1 同款流程：build → test → 真机实测 → commit → tag → push。

---

## 三、阶段 3（v2.0.0）的具体执行步骤

> 工程出货 + GRDB 改 SPM + 签名 + 测试扩展。

### Step 16：修 §3.1 CODE-2（GRDB 改 SPM）

#### 改动 16.1：`project.yml:26-29`

```yaml
# old
packages:
  GRDB:
    path: /Users/xinmin/Developer/Vendor/GRDB.swift

# new
packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift
    from: "7.0.0"
```

#### 改动 16.2：删除本地 GRDB 路径

```bash
# 仅删除 vendored 副本（如果存在）
ls /Users/xinmin/Developer/Vendor/GRDB.swift   # 确认路径
# 不需要删本地文件——只是 project.yml 不再引用它
```

#### 改动 16.3：重新生成 Xcode 工程

```bash
xcodegen generate
# 期望：重新生成 KaJi.xcodeproj
```

#### 改动 16.4：build + 干净机器验证

```bash
# 在干净目录 clone + xcodegen + build
cd /tmp && git clone /Users/xinmin/openmind/03_Own_project/16-KaJi test-kaji-spm
cd test-kaji-spm && xcodegen generate
xcodebuild -project KaJi.xcodeproj -scheme KaJi -destination 'platform=macOS' build
# 必须 BUILD SUCCEEDED
```

### Step 17：修 §3.3 版本号单一源

#### 改动 17.1：`project.yml` 设为单一事实源

```yaml
# project.yml 已有
MARKETING_VERSION: "1.6.2"   # 当前
CURRENT_PROJECT_VERSION: "26"
```

#### 改动 17.2：从 pbxproj 删除硬编码

删除 pbxproj 中的 `MARKETING_VERSION` 与 `CURRENT_PROJECT_VERSION` 行，让 xcodegen 注入。

### Step 18：修 §3.2 签名 + 公证

#### 改动 18.1：`project.yml`

```yaml
settings:
  base:
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: "Developer ID Application"   # 替换 "-"
    DEVELOPMENT_TEAM: "<YOUR_TEAM_ID>"                # 替换 ""
```

#### 改动 18.2：公证流程

```bash
# 用 notarytool（macOS 13+）
xcrun notarytool submit KaJi.dmg \
    --apple-id <APPLE_ID> \
    --team-id <TEAM_ID> \
    --password <APP_SPECIFIC_PASSWORD> \
    --wait
```

### Step 19：修 §3.6 URL 检查

#### 改动 19.1：`KaJi/Views/Settings/SettingsView.swift:161`

```swift
// 确认仓库 URL 是否正确（git remote -v）
// 把 <CORRECT_URL> 替换为真实地址
guard let url = URL(string: "<CORRECT_URL>/releases") else { return }
```

### Step 20：阶段 3 总收口（v2.0.0 推送）

**`MARKETING_VERSION` → `2.0.0` / `CURRENT_PROJECT_VERSION` → `27`**

按 v1.6.1 同款流程：build → test → 干净机器实测 → Gatekeeper 实测 → commit → tag v2.0.0 → push。

---

## 四、不做的清单（伪需求，避免分散注意力）

- ❌ NSTextView 重写编辑器
- ❌ Tree-sitter / 语法高亮 / Vim Mode
- ❌ iCloud / CloudKit 同步
- ❌ 插件系统 / 国际化 / 多语言
- ❌ GRDB 替换为裸 SQLite3
- ❌ Actor 化 `CardSearchIndex`
- ❌ 自动更新（Sparkle）

---

## 五、阶段间的硬约束（重复强调）

1. **阶段 1 不先建测试，阶段 2 的 ARCH-2 增量改了 commitDraft 路径但没有"往返恒等"测试兜底，下次有人改 CardType 又会破**
2. **阶段 2 不等阶段 1 收口，ARCH-2 增量改了 commitDraft 后的刷新逻辑，与 BUG-1 退出 flush 改造在同一个函数打架**
3. **阶段 3 不等阶段 2，改了 GRDB 依赖、签名、上线，发现性能问题要改代码，但代码已经签好名了**

每阶段推送前必须满足：
- [ ] `xcodebuild build` BUILD SUCCEEDED
- [ ] `xcodebuild test` 全绿
- [ ] 真机实测清单逐项通过
- [ ] 已确认功能清单全部守住
- [ ] 你说"可以推"

---

## 六、给执行者的纪律提醒（我对自己）

我承诺：
- 每改一处立刻 build，**绝不**攒一堆一起 build
- 每改一处立刻向你汇报，**绝不**自动推进到下一步
- 遇到与方案不符的代码事实，**立刻**停下来告诉你，**绝不**硬改
- 每次 commit 前 grep 验证无死代码、无调用方遗漏
- 每次推送前必须由你说"可以推"

如果你觉得这份计划里某一步风险高，告诉我，我会把它拆得更细，或者改用更保守的方案。
